export const ESL = {
  READY_TIMEOUT_MS: 5000,
  MAX_RECONNECT_DELAY_MS: 30000,
};

export const JOB = {
  TTL_MS: 15000,
};

export const REGISTRY = {
  CALL_TTL_MS: 5 * 60 * 1000,
  JOB_TTL_MS: 60 * 1000,
  SWEEP_INTERVAL_MS: 30 * 1000,
};
